def recursive_sum(arr, index=0):
    if index == len(arr):
        return 0
    else:
        return arr[index] + recursive_sum(arr, index + 1)

arr = [12, 13, 14, 15]
print(recursive_sum(arr))  # Output: 54
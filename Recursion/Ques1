def sum_of_digits(n):
    sum = 0
    while n:
        sum += n % 10
        n //= 10
    return sum

num = int(input("Enter the digit:"))
result = sum_of_digits(num)
print("Sum of digits in", num, "is", result)